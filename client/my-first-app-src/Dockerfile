# Шаг 1: Взять за основу официальный, легковесный образ Python 3.9
FROM python:3.9-slim

# Шаг 2: Установить нашу единственную зависимость - Flask
RUN pip install Flask==2.2.2 "Werkzeug<3.0.0"
 # Указываем версию для воспроизводимости

# Шаг 3: Скопировать наш скрипт app.py с хост-машины внутрь образа в корневую папку /
COPY app.py /app.py

# Шаг 4: Указать команду, которая должна быть выполнена при запуске контейнера.
# Флаг -u важен, он отключает буферизацию вывода, и мы будем видеть print() сразу.
CMD ["python3", "-u", "/app.py"]
